.TH wadd2 1W "2002.5.31" "WIN SYSTEM" "WIN SYSTEM"
.SH 名称
wadd2 - 時間の重なった２つの win 形式ファイルの統合
.SH 形式
wadd2
.I mainfile
.I subfile
[
.I outdir
]
.LP
.SH 解説
wadd2 は、win 形式ファイル
.I \ mainfile
と、
win 形式ファイル
.I \ subfile
を統合して、結果を
.I \ mainfile
と同じ名前で
.I \ outdir
で指定されたディレクトリに入れます。
.I \ outdir
として '\-'を指定すると結果は標準出力に書き出されます。
.I \ outdir
が指定されないと、結果は
.I \ mainfile
を置き換えます。同じ秒の同じチャネルのデータが両方のファイルに存在する
場合は、
.I \ mainfile
のデータが優先され、
.I \ subfile
のデータは捨てられます。それ以外の場合はすべてのデータが
統合されて結果のファイルに出力されます。
したがって、結果のファイルの時間範囲は、２つの入力ファイルの
うち早く始まったほうの開始時間から、遅く終わったほうの終了時間までに
なります。
.LP
同じチャネル構成をもち、時間範囲が前後して重ならない２つの win 形式
ファイルを連結するには、(wadd2 も利用できますが) cat(1) が利用できます。
.LP
wadd2 は引数なしで起動すると簡単な使用法を表示します。
.SH オプション
.TP 
なし
.SH ソース 
.TP
`wadd2.c'
.SH 関連事項
wadd.c(1W), win(1W), wed(1W), wck(1W), winformat(1W)
